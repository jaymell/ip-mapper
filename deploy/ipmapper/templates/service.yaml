apiVersion: v1
kind: Service
metadata:
  name: {{ include "ipmapper.fullname" . }}
  labels:
    app: {{ include "ipmapper.name" . }}
    chart: {{ include "ipmapper.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "ipmapper.name" . }}
    release: {{ .Release.Name }}
